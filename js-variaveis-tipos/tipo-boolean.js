// Tipo boolean

const primeiroNumero = 20;
const segundoNumero = 10;

// Variável assumindo valores de boolean
let isCadastroAtivo = true;


/**
 * Pode ser comparado não só condições mas todos e qualquer
 * tipo de variavel desde que seja válido e aceitavél
 */

console.log(isCadastroAtivo);

// Faz a compração utilizando o operador '==='
console.log(primeiroNumero === segundoNumero); // FALSE

// Faz a comparação utilizando o operador '!=='
console.log(primeiroNumero !== segundoNumero); // TRUE
